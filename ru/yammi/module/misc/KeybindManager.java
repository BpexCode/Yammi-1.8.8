package ru.yammi.module.misc;

import ru.yammi.config.Config;
import ru.yammi.gui.keybind.KeybindScreen;
import ru.yammi.module.Category;
import ru.yammi.module.Module;

//public class KeybindManager extends Module {

    /*private KeybindScreen keybindScreen;

    public KeybindManager() {
        super("KeybindManager", Category.Misc, "Open keyboard for bind modules");
    }

    @Override
    public void onEnable() {
        if(this.mc.currentScreen != null)
            this.mc.displayGuiScreen(null);
        this.mc.displayGuiScreen(getKeybindScreen());
        this.setState(false);
        Config.store();
    }

    @Override
    public void onDisable() {
        if(this.mc.currentScreen != null && this.mc.currentScreen instanceof KeybindScreen)
            this.mc.displayGuiScreen(null);
    }

    public KeybindScreen getKeybindScreen(){
        if(keybindScreen == null)
            keybindScreen = new KeybindScreen();
        return keybindScreen;
    }*/
//}
